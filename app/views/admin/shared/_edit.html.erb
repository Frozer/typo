<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<% if current_user.admin? %>
  <div class=''>
  <h4>Merge Articles</h4>
  <%= form_tag "/admin/content/merge_with/#{@article.id}", :method => :put do %>
    Article ID
    <input id="withid" name="merge_with" size="30" type="text" />
	<%= submit_tag 'Merge' %>
  <% end %>
  </div>
<% end %>
